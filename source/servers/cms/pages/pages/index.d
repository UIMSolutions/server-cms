module servers.cms.pages.pages.index;

@safe:
import servers.cms;

void uimPagesIndex(HTTPServerRequest req, HTTPServerResponse res) {
  debugMethodCall(moduleName!uimPagesIndex~":uimPagesIndex");

  // CMSPagesIndexPageController(thisServer).request(req, res);
}