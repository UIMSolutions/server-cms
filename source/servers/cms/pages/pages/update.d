module servers.cms.pages.pages.update;

@safe:
import servers.cms;

void uimPagesEdit(HTTPServerRequest req, HTTPServerResponse res) {
  debugMethodCall(moduleName!uimPagesEdit~":uimPagesEdit");

  // CMSPagesUpdatePageController(thisServer).request(req, res);
}