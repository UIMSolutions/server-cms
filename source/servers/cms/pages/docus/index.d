module servers.cms.pages.docus.index;

@safe:
import servers.cms;

void uimDocusIndex(HTTPServerRequest req, HTTPServerResponse res) {
  debugMethodCall(moduleName!uimDocusIndex~":uimDocusIndex");

  // CMSDocusIndexPageController(thisServer).request(req, res);
}