module servers.cms.pages.docus.update;

@safe:
import servers.cms;

void uimDocusEdit(HTTPServerRequest req, HTTPServerResponse res) {
  debugMethodCall(moduleName!uimDocusEdit~":uimDocusEdit");

  // CMSDocusUpdatePageController(thisServer).request(req, res);
}