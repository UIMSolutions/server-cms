module servers.cms.pages.docus.read;

@safe:
import servers.cms;

void uimDocusView(HTTPServerRequest req, HTTPServerResponse res) {
  debugMethodCall(moduleName!uimDocusView~":uimDocusView");

  // CMSDocusReadPageController(thisServer).request(req, res);
}